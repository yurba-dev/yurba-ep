{
  "name": "yurba-ep",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:js && npm run build:css",
    "build:js": "esbuild yurba-ep.js --bundle=false --minify --outfile=dist/yurba-ep.min.js",
    "build:css": "esbuild yurba-ep.css --minify --outfile=dist/yurba-ep.min.css"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
